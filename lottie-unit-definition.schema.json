{
  "$id": "lottie-unit-definition@1.1",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Verona Player Lottie Unit Definition",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "backgroundColor",
    "scenes",
    "animations"
  ],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for the unit definition"
    },
    "version": {
      "type": "string",
      "description": "Version of the unit definition format"
    },
    "backgroundColor": {
      "type": "string",
      "pattern": "^#([A-Fa-f0-9]{3}){1,2}$",
      "description": "Background color of the unit in hex format"
    },
    "scenes": {
      "type": "array",
      "description": "Array of scenes, each with different background and layout settings",
      "items": {
        "$ref": "#/definitions/SceneData"
      }
    },
    "animations": {
      "type": "array",
      "description": "List of all animations used",
      "items": {
        "$ref": "#/definitions/AnimationData"
      }
    }
  },
  "definitions": {
    "SceneData": {
      "type": "object",
      "required": [
        "scene",
        "script"
      ],
      "properties": {
        "scene": {
          "type": "string",
          "description": "identifier of scene"
        },
        "cockpitSrc": {
          "type": "string",
          "description": "Image File (best svg or transparent png) to use on top of the animations",
          "contentEncoding": "base64"
        },
        "backgroundIds": {
          "type": "array",
          "description": "Array of animations as Ids in the background for the complete scene",
          "items": {
            "type": "string"
          }
        },
        "foregroundIds": {
          "type": "array",
          "description": "Array of animations as Ids in the foreground for the complete scene",
          "items": {
            "type": "string"
          }
        },
        "animationSlots": {
          "type": "array",
          "description": "Definition of the clots available for the animations. Each slot work as a single Lottie Player instance",
          "items": {
            "$ref": "#/definitions/animationSlotData"
          }
        },
        "script": {
          "type": "array",
          "description": "Array of animations and audio to run after each other",
          "items": {
            "$ref": "#/definitions/ScriptData"
          }
        },
        "interaction": {
          "type": "boolean",
          "description": "Toggle if an interaction should be overlayed",
          "default": false
        },
        "interactionType": {
          "type": "string",
          "description": "Type of Interaction",
          "enum": ["BUTTONS"],
          "default": "BUTTONS"
        },
        "interactionParameters": {
          "$ref": "#/definitions/InteractionParameters"
        }
      }
    },
    "ScriptData": {
      "type": "object",
      "required": [
        "scene",
        "animationIds"
      ],
      "properties": {
        "scene": {
          "type": "string",
          "description": "identifier of this part of the scene"
        },
        "loopTarget": {
          "type": "string",
          "description": "Id of the animation slot to listen for loop completed"
        },
        "loopCount": {
          "type": "integer",
          "description": "Number of loops to be played (zero for unlimited)",
          "default": 0
        },
        "animationIds": {
          "type": "array",
          "description": "Array of animations to be shown",
          "items": {
            "$ref": "#/definitions/SceneAnimationData"
          }
        },
        "audioSrc": {
          "type": "string",
          "description": "Audio to be played along the animations",
          "contentEncoding": "base64"
        },
        "waitForAudioToFinish": {
          "type": "boolean",
          "description": "Wait for the audio to finish to proceed to next animation/scene",
          "default": false
        }
      }
    },
    "animationSlotData": {
      "type": "object",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "identifier of the animation slot"
        },
        "zindex": {
          "type": "number",
          "description": "z-index of the animation slot for layering"
        }
      }
    },
    "SceneAnimationData": {
      "type": "object",
      "required": [
        "id",
        "animationId"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Id of the animation slot the animation should be put in"
        },
        "animationId": {
          "type": "string",
          "contentEncoding": "Id of animation used as identifier"
        },
        "loop": {
          "type": "boolean",
          "description": "Boolean if the animation should run in a loop",
          "default": true
        }
      }
    },
    "AnimationData": {
      "type": "object",
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "string",
          "description": "Id of animation used as identifier"
        },
        "animationSrc": {
          "type": "string",
          "description": "animation Source for a single animation",
          "contentEncoding": "base64"
        },
        "animations": {
          "type": "array",
          "description": "array of possible animations to choose from",
          "items": {
            "$ref": "#/definitions/AnimationData"
          }
        },
        "parameterId": {
          "type": "string",
          "description": "Id of the SharedParameter to choose which animation has to be used"
        }
      }
    },
    "InteractionParameters": {
      "type": "object",
      "required": [
        "sharedId",
        "options"
      ],
      "properties": {
        "sharedId": {
          "type": "string",
          "description": "Id of the SharedParameter, the answer will be stored as"
        },
        "options": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/InteractionOptions"
          }
        }
      }
    },
    "InteractionOptions": {
      "type": "object",
      "required": [
        "value"
      ],
      "properties": {
        "imageSrc": {
          "type": "string",
          "description": "image or lottie file",
          "contentEncoding": "base64"
        },
        "label": {
          "type": "string"
        },
        "value": {
          "type": "string"
        },
        "speed": {
          "type": "number",
          "description": "speed of animation when lottie animation is provided as ImageSrc",
          "default": 1
        }
      }
    }
  }
}
